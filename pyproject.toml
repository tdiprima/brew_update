[project]
name = "brew-update"
version = "0.1.0"
description = "Python scripts that run a full Homebrew update/upgrade/cleanup pass, pulling the sudo password for cask upgrades from the macOS keychain."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
